version: '3.7'
services:
  myapp:
    image: kennethreitz/httpbin:latest
    deploy:
      replicas: 2
      labels:
        - henk=ingrid
    ports:
      - 80
    networks:
      - edge-traffic

networks:
  edge-traffic:
    external: true